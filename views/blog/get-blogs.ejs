<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Posts - BlogApp</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gradient-to-br from-indigo-500 to-purple-600 min-h-screen font-sans">
    <!-- Navbar -->
    <nav class="bg-gray-800 shadow sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <a href="#" class="text-white text-2xl font-bold flex items-center gap-2">
                    📝 BlogApp
                </a>
                <div class="hidden md:flex space-x-4">
                    <a href="#" class="text-white px-3 py-2 rounded-md text-sm font-medium bg-indigo-600">Home</a>
                    <a href="/add-blogs" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700">Create Post</a>
                    <a href="/get-blogs" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700">My Posts</a>
                    <a href="/" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700">Categories</a>
                    <a href="#" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700">About</a>
                    <a href="#" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-700">Contact</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Page Header -->
    <header class="text-center py-12 text-white">
        <h1 class="text-4xl md:text-5xl font-bold mb-3">Latest Blog Posts</h1>
        <p class="text-lg font-light">Discover amazing stories and insights from our community</p>
    </header>

    <!-- Blog Container -->
    <main class="max-w-4xl mx-auto px-4 space-y-8">
        <% if (typeof blog !== 'undefined' && blog.length > 0) { %>
            <% blog.forEach(function(blog, index){ %>
                <div class="bg-white rounded-lg shadow-md overflow-hidden">
                    <div class="p-6">
                        <div class="flex justify-between items-start">
                            <div>
                                <h2 class="text-2xl font-bold text-gray-800 mb-2" id="blog<%= index %>" name="blog<%= index %>">
                                    <%= blog.title %>
                                </h2>
                                <% if (blog.subtitle) { %>
                                    <p class="text-gray-600 italic mb-4"><%= blog.subtitle %></p>
                                <% } %>
                            </div>
                            <div class="flex space-x-2">
                                <a href="/edit/<%- blog.id %>" title="Edit">
                                    <button class="text-indigo-600 hover:text-indigo-800">
                                        ✏️
                                    </button>
                                </a>
                                <a href="/delete/<%- blog.id %>" title="Delete">
                                    <button class="text-red-600 hover:text-red-800">
                                        🗑️
                                    </button>
                                </a>
                            </div>
                        </div>
                        <p class="text-gray-700 mb-4 line-clamp-3">
                            <%= blog.content %>
                        </p>
                        <div class="flex justify-between items-center text-sm text-gray-500">
                            <div class="flex gap-3 items-center">
                                <span>👤 <%= blog.authorName %></span>
                                <span>📅 <%= blog.publishDate %></span>
                            </div>
                            <a href="/blog/<%- blog.id %>" class="text-indigo-500 hover:underline">Read More →</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <div class="text-center text-white py-20">
                <div class="text-6xl mb-4">📝</div>
                <h3 class="text-xl font-semibold">No blog posts yet</h3>
                <p class="mb-6">Get started by creating your first blog post</p>
                <a href="/blog/add" class="bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-3 rounded-md font-medium inline-flex items-center gap-2">
                    ✍️ Write Your First Post
                </a>
            </div>
        <% } %>
    </main>

    <!-- Floating Action Button -->
    <a href="/add-blogs" class="fixed bottom-6 right-6 bg-indigo-600 hover:bg-indigo-700 text-white w-14 h-14 flex items-center justify-center rounded-full shadow-lg text-2xl">
        ✏️
    </a>
</body>
</html>
